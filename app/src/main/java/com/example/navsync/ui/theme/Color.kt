package com.example.navsync.ui.theme

import androidx.compose.ui.graphics.Color

// ==========================================
// NAVSYNC DARK OBSIDIAN & NEON GREEN THEME
// ==========================================

// Pitch/Obsidian Black Surfaces
val DarkBg = Color(0xFF050709)            // Deep pitch black background
val DarkSurface = Color(0xFF0C1014)       // Card and panel surface
val DarkElevated = Color(0xFF12181F)      // Elevated sheets, HUD, popups
val DarkContainer = Color(0xFF0E141A)     // Inset input fields & sub-panels
val DarkContainerSubtle = Color(0xFF151D24) // Hover or chip background

// Vibrant Neon & Emerald Greens
val NeonGreen = Color(0xFF00E676)          // Primary active, metrics, pins
val EmeraldGreen = Color(0xFF10B981)       // Smooth secondary green
val DarkGreenBg = Color(0xFF052B19)        // Icon circle and chip background
val DarkGreenBorder = Color(0xFF0E4329)    // Subtle emerald borders
val GreenGlow = Color(0x3300E676)          // Translucent green glow halo

// Thin & Modern Borders
val BorderDark = Color(0xFF1A232B)         // Subtle 1.dp dark card borders
val BorderGreenSubtle = Color(0xFF163E2A)  // Subtle 1.dp emerald active borders
val BorderGreenBright = Color(0xFF00E676)  // Highlighted border

// Neutral Text Hierarchy
val TextPrimary = Color(0xFFFFFFFF)        // Pure white high-contrast text
val TextSecondary = Color(0xFF94A3B8)      // Modern slate silver text
val TextMuted = Color(0xFF64748B)          // Subdued metadata & unit text
val TextDark = Color(0xFF050709)           // Deep black for text on NeonGreen buttons

// Functional Alerts
val AccentAmber = Color(0xFFF59E0B)        // Warnings, degraded states, dead reckoning
val DarkAmberBg = Color(0xFF331C04)
val AccentRed = Color(0xFFEF4444)          // Cancel, exit, offline loss
val DarkRedBg = Color(0xFF310B0B)

// Location & Navigation Blues
val ElectricBlue = Color(0xFF00B0FF)       // Vibrant GPS location puck, cone, recenter icon
val DarkBlueBg = Color(0xFF0A2239)         // Location button background
val BorderBlueSubtle = Color(0xFF14456B)   // Location button subtle active border